// const spicedPg = require("spiced-pg");

// const db = spicedPg(`postgres:postgres:postgres@localhost:5432/petition`);

// function getPetition(first, last) {
//     return db
//         .query(
//             `SELECT * FROM first
//     // WHERE last = $1 AND country = $2`
//             [first, last]
//         )
//         .then(({ rows }) => {
//             console.log("---->>", rows);
//         })
//         .catch((err) => console.log("error", err.message));
// }

// // getPetition("Pri");
